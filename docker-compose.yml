version: '3.8'
services:
  zeek:
    image: zeek/zeek:latest
    container_name: zeek-mcp-container
    volumes:
      - ./zeek-mcp:/workspace  # Монтируем конкретную директорию zeek-mcp
    working_dir: /workspace
    tty: true
    stdin_open: true
    profiles: ["zeek"]